# Project Customizations

## Build and Commit Process
Rule: Always show expanded details and do not collapse thought processes.
Rule: When a change is ready, you must ALWAYS run a build (e.g. `npm run build`) so that the rebuilt files in the `dist` folder are updated. 
Rule:Do NOT commit the changes yourself, unless the human asks you to.
Rule: Commit messages should be IN DETAIL.
